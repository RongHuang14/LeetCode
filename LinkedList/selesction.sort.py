def selection_sort(arr):
    """
    sort the given array in place
    """
    N = len(arr)
    # for each sport
    for idx in range(N): # 0...N-1
        # find the smallest element in the remaining 
        # and swap into the current spot
        
